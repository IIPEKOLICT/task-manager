export enum EndPoint {
  TODOS = 'todos',
  TOPICS = 'topics',
  TAGS = 'tags',
}

export enum Selector {
  ID = ':id',
  TOPIC_TODOS = ':topicId/todos',
}

export enum Priority {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}
