import 'package:flutter/material.dart';

class BottomBarData {
  final String label;
  final Color color;
  final Widget icon;
  final Widget content;

  const BottomBarData(this.label, this.color, this.icon, this.content);
}